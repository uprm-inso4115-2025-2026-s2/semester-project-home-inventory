# Entities

Contains plain domain objects that represent the core business data structures. Entities are framework-independent and hold no serialization or UI logic. They define the shape of data as the rest of the app understands it.
