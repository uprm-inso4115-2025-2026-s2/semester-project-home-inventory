# Data Repositories

Contains concrete implementations of the repository interfaces defined in the domain layer. These classes coordinate between data sources to fulfill data requests. They handle mapping data models to domain entities.
