# Data Models

Contains data transfer objects (DTOs) that represent the shape of data from external sources like APIs or databases. Models handle serialization and deserialization (e.g., `fromJson`, `toJson`). They extend or map to domain entities but include data-layer-specific logic.
