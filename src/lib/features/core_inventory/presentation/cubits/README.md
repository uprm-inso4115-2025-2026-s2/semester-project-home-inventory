# Cubits

Contains Bloc/Cubit classes and their corresponding state classes for state management. Cubits invoke use cases and emit new states in response to user actions or data changes. Each cubit manages the state for a specific part of the feature's UI.
