enum InviteStatus { pending, accepted, rejected, revoked }

enum MemberRole { viewer, editor, owner }

enum SpendSource { manual, scan, import, adjustment, refund }

enum BudgetPeriod { weekly, monthly, yearly }

enum SplitMode { equal, weighted, custom }
