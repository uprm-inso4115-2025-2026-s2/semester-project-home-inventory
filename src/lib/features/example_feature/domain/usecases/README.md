# Use Cases

Contains single-responsibility classes that each encapsulate one business action (e.g., create, update, delete). Use cases orchestrate domain logic by interacting with repository abstractions. The presentation layer calls these to perform operations.
