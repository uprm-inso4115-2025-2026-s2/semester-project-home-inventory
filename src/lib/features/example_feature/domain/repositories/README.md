# Domain Repositories

Contains abstract repository interfaces that define the data contracts the domain layer expects. These are implemented by the data layer, enforcing the dependency inversion principle. Use cases depend on these abstractions, not on concrete implementations.
