# Presentation Layer

Contains all UI-related code including pages, widgets, cubits (state management), and route definitions. This layer depends on the domain layer's use cases to trigger business logic. No data-fetching or business rules should exist here.
