# Widgets

Contains reusable, self-contained UI components specific to this feature. Widgets receive data via constructor parameters and emit user interactions via callbacks. Keep them focused on rendering; avoid placing business logic here.
