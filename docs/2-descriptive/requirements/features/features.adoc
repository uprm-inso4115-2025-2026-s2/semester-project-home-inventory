=== Features

Features describe user-visible capabilities of the system-to-be above the level of individual requirements. The list is intentionally concise for Milestone 1 and grounded in the alert/grocery-list artifacts (auto-check output, trigger priorities, grocery list user flow).

[cols="1,3,2,3",options="header"]
|===
|ID |Feature |Primary value |Related stories / notes

|F-INV
|Inventory tracking (create/update items)
|Enables a reliable tracked view of household items (quantity, location, ownership, optional expiration).
|Supports US-INV-* (core inventory stories). Basis for alerts and grocery list generation.

|F-ALERT
|Alert generation from actionable state changes
|Notifies members when tracked conditions become actionable (e.g., LOW_STOCK, OUT_OF_STOCK, NEAR_EXPIRY/EXPIRED), with deduplication based on state transitions.
|Supports US-ALT-* and the "alert trigger logic" notes. Delivery can be in-app initially; push may be later.

|F-AUTOCHECK
|Automated daily check (AutoCheck) over inventory
|Produces an "AutoCheck Output" (items + status results) that drives alerts and grocery list updates without manual inspection.
|Grounded in the AutoCheck/alert artifacts. Verification focuses on boundary cases (threshold equality, expiration boundaries) and idempotency.

|F-GL-AUTO
|Auto-generated grocery list from AutoCheck Output
|Keeps a grocery list updated based on priority triggers, reducing missed restocks and duplicate planning.
|Consumes AutoCheck Output. Priority triggers (in order): (1) OUT_OF_STOCK, (2) EXPIRED, (3) LOW_STOCK, (4) FREQUENT & (LOW/OUT). Items already on the list are skipped. Actions are logged.

|F-GL-USER
|Grocery list review and editing flow
|Users review auto-added items, then remove/edit/add items manually; the updated list is saved automatically.
|Based on the grocery list user flow artifacts. Emphasizes user override and preventing clutter.

|F-SPEND (optional scope if budgeting is in Milestone 1)
|Spending capture and basic summaries
|Lets members record spending and view simple period summaries (shared vs personal), supporting budget awareness.
|Supports US-SB-* and US-RPT-* (basic reporting). Detailed finance flows can be deferred if needed.
|===

[NOTE]
====
F-GL-AUTO and F-GL-USER are based on the provided grocery list generator and user flow artifacts. Implementation details such as UI layout and backend scheduling are deferred to Section 2.3; here we capture only the user-visible capability and the trigger/priority semantics.
====